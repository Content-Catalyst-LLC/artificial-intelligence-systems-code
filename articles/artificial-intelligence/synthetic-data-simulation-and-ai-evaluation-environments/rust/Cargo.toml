[package]
name = "synthetic_evaluation_validator"
version = "0.1.0"
edition = "2021"
description = "Rust CLI for validating synthetic data and simulation evaluation records."

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
