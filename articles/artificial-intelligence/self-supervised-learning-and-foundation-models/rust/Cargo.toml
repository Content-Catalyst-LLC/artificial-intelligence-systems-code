[package]
name = "foundation_model_metadata_validator"
version = "0.1.0"
edition = "2021"
description = "Rust CLI for validating foundation model and pretraining metadata."

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
